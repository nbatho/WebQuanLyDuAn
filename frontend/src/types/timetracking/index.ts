export interface TimeEntry {
    id: string;
    task: string;
    space: string;
    spaceColor: string;
    duration: string;
    durationMs: number;
    date: string;
    user: string;
    userColor: string;
    isRunning: boolean;
}
